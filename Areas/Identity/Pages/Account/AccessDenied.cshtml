@page
@model AccessDeniedModel
@{
    ViewData["Title"] = "Erişim Engellendi";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6 text-center">
            <div class="card shadow-lg border-0 rounded-4 p-5">
                <i class="bi bi-shield-lock-fill text-danger display-1 mb-4"></i>
                <h1 class="fw-bold text-dark">DUR!</h1>
                <h3 class="text-danger">@ViewData["Title"]</h3>
                <p class="text-muted">Bu bölgeye erişim yetkiniz bulunmamaktadır. Lütfen yönetici ile iletişime geçin.</p>
                <div class="mt-4">
                    <a href="/" class="btn btn-dark btn-lg px-5 fw-bold rounded-pill">Anasayfaya Dön</a>
                </div>
            </div>
        </div>
    </div>
</div>